# Paths never published to the Pages site (rsync --exclude-from syntax; a
# leading / anchors the pattern to the repo root). The published gh-pages tree
# should contain only servable site content — repo docs and dev/CI tooling
# stay on GitHub. THIRD_PARTY_LICENSES.md is deliberately published: license
# attribution ships with the bundled third-party assets it covers.
.git
.github
_site
# .gitignore / .gitattributes at any depth
.git*
/.pre-commit-config.yaml
/CHANGELOG.md
/CONTRIBUTING.md
/README.md
# Dev/QA harness pages — excluded from production, but the preview job
# includes them (--include '/test/***' ahead of this list), so QA against a
# deployed copy happens on PR previews.
/test
# Per-tool CI config (validate.sh / smoke.json)
/tools/*/ci
# Per-tool dev files — mirrors what each tool's own make-snapshot.py already
# omits from its served versions/ snapshots (data/ stays: tools read it).
/tools/*/CHANGELOG.md
/tools/*/scripts
/tools/*/package.json
